# TODO看板

## ✅ 已完成

- 基于规则的细粒度文件轮转，支持正则匹配的文件/目录轮转，按规则分组管理
- 项目架构重构，引入任务注册中心与应用上下文，核心模块拆分为多个子模块
- 编写了完整文档，解释和说明了项目的所有必要信息

## 🧪 测试中

- 支持通过 HTTP 连接远程目标，客户端通过 API 上传文件到远程目录
  - 将 HTTP 超时时间转为可配置项
  - HTTP 传输出错时自动退避
  - HTTP 服务器端支持并发处理请求

## 🚧 正在做

## 📋 已计划

- 增加 `ssh_remotes` 配置，专门用于 sync 模式连接远程目标
- 更新 README，匹配最新变更

## 🔍 评估中

- 前端界面
- 允许用户在任务前、后执行自定义命令或脚本
- rotate 模式下以存储设备的可用空间为阈值
- 多语言支持
- HTTP 传输模式下的断点续传
- 在所有模式下都允许 `dest` 置空（Dry Run）

## ❌ 不会做

## 🗑️ 已放弃

- 通过合并多步操作的方式减少 HTTP 往返次数

    1. 实测多次往返的开销并不大。
    2. 有些逻辑在客户端侧进行决策，有些逻辑又让服务器端进行决策，不仅逻辑解耦困难，而且彼此之间没有可预测的分工原则，反倒不如不将任何决策环节放到服务器端，保持代码逻辑的干净统一和可预测性。
    
- 将 HTTP 服务器迁移至生产服务器
  
    1. waitress 在接收文件时，会将文件先缓存至系统分区中，接收完毕再移动至目标目录。这种工作方式严重违反了 SmartArchiver 的需求场景：
    
        1. 会造成严重的写放大
        2. 有耗干系统分区空间的风险
        3. 对系统所在盘造成完全不必要的写寿命损耗
        4. 浪费时间
  
    2. 为什么不用 gunicorn 等方案呢？

        答：过度工程化，没有必要。
  
    因此，除非找到更好的方案，否则该项目会继续使用 `app.run()` 来启动服务器，这对于该工具的使用场景来说已经完全足够。
